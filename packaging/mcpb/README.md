# PeekWin Claude Desktop Extension

This bundle runs the packaged `peekwin` binary locally inside Claude Desktop and exposes PeekWin's MCP tools over stdio.

Install:

1. Open Claude Desktop
2. Open `Settings`
3. Open `Extensions`
4. Open `Advanced settings`
5. Choose `Install Extension...`
6. Select the `.mcpb` file for your Windows architecture
